{
  "name": "gerpi-monitoring",
  "version": "1.0.0",
  "private": true,
  "description": "GERPI Monitoring — Loyihalarni amalga oshirish guruhlari (PIU) faoliyatini markazlashtirilgan nazorat qilish milliy platformasi",
  "scripts": {
    "setup": "npm --prefix backend install && npm --prefix backend run migrate && npm --prefix backend run seed",
    "dev": "npm --prefix backend run dev",
    "start": "npm --prefix backend start",
    "migrate": "npm --prefix backend run migrate",
    "seed": "npm --prefix backend run seed"
  }
}
